# Worklog - 2026-04-01

## 1) Repository Scan Summary

### Core runtime files
- `app.py`: Streamlit UI, challenge generation/verification, batch experiment persistence.
- `mqtt_bridge.py`: file-IPC <-> MQTT bridge, heartbeat writer, response forwarder.
- `node.py`: simulated device subscriber/responder.
- `config.py`: broker/topic and run-time knobs.

### Data and artifacts
- `authentication_history.db`: source of truth for auth and batch statistics.
- `challenge_out.json`, `response_in.json`, `bridge_status.json`: runtime IPC and health files.
- `artifacts/`: generated reports and experiment outputs.

### Documentation clusters
- `docs/guides/`: how-to and troubleshooting.
- `docs/phases/`: milestone status docs.
- `docs/reports/`: technical/reporting outputs.

## 2) Today Progress (Meaningful Changes)

### A. Connection stability and bridge reliability
- Improved bridge startup/reconnect behavior in `mqtt_bridge.py`:
  - Use async connect and MQTT loop reconnect handling.
  - Removed aggressive reconnect call pattern inside polling loop.
  - Added payload forwarding fields (`nonce`, `max_response_time`) for anti-replay consistency.
- Result: bridge can stay connected more stably without repeated connect thrash.

### B. One-click verification now honors anti-replay path
- Updated one-click flow in `app.py`:
  - Dynamic-seed mode now generates seed/nonce/timestamp.
  - Seed record is persisted in session store before sending challenge.
  - Challenge send includes nonce/timestamp/timeout metadata.
  - Verification enforces nonce consistency and replay protections.
- Result: one-click mode no longer bypasses dynamic-seed anti-replay logic.

### C. Batch experiment statistics enriched
- Extended `batch_experiments` persistence to include:
  - `pass_rate`, `hd_std`, `hd_p10`, `hd_p90`.
- Added startup-time compatibility migration for older DB schema.
- Result: better statistical visibility and reporting depth for experiments.

### D. Data authenticity check performed
- Verified a reported batch directly from SQLite (`batch_1775030540003`) against UI totals.
- Result: displayed values are DB-backed, not fabricated UI-only numbers.

## 3) Current Risks / Noise

### A. Git noise from tracked virtualenv files
- Current working tree is dominated by `.venv` package churn (many modified/deleted files).
- This obscures real code changes and increases commit risk.

### B. Multi-process run hygiene
- Multiple bridge/node instances can overwrite IPC files or heartbeat status.
- This can make UI status appear inconsistent.

## 4) Next Modification Plan (Next Session)

## Priority 1 - Clean version control surface (must do first)
1. Ensure only source/docs are staged in commits.
2. Keep `.venv/` out of future commits (if currently tracked, untrack once in a dedicated cleanup commit).
3. Add a small pre-commit checklist section in docs to prevent environment-file commits.

## Priority 2 - Runtime consistency hardening
1. Add a startup guard for duplicate process instances (bridge/node).
2. Add clear status banner in UI for bridge state source and last heartbeat time.
3. Add a short timeout and user-facing reason when response nonce mismatch occurs.

## Priority 3 - Verification and test coverage
1. Add targeted tests for one-click dynamic-seed anti-replay path.
2. Add bridge reconnect regression test scenario.
3. Add validation script to assert batch metrics fields are always populated.

## Priority 4 - Documentation sync
1. Update `docs/guides/MQTT_FIX_GUIDE.md` with latest reconnect model.
2. Add one-click dynamic-seed flow diagram and expected logs.
3. Update report docs to include new HD distribution metrics definitions.

## 5) Suggested Next Session Command Order
1. Start clean: one bridge, one node, one app process only.
2. Run one-click dynamic-seed smoke test and capture logs.
3. Run small batch test (n=20) to validate new metric fields.
4. Commit only source/docs changes.

## 6) Definition of Done for Next Session
- One-click dynamic-seed pass/fail behavior is reproducible and logged.
- Batch row includes `pass_rate`, `hd_std`, `hd_p10`, `hd_p90`.
- No `.venv` files appear in staged diff.
- Docs updated for reconnect and anti-replay one-click flow.


