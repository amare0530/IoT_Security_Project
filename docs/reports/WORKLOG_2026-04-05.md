# Worklog - 2026-04-05

## 今日進階整理（已完成）

1. 進一步清理執行期中繼檔
- 已刪除 challenge_out.json
- 已刪除 response_in.json
- 已刪除 bridge_status.json（若存在）

2. 清理專案本地快取
- 已刪除專案中的 __pycache__（不含 .venv 內套件快取）

3. 防止未來再出現本機噪音
- 已更新 .gitignore，新增：
  - .vscode/
  - .streamlit/
  - .pytest_cache/
  - *.log

4. 保留且未動到的重要檔案
- 核心程式：app.py, mqtt_bridge.py, node.py
- 文件與報告：docs/, README.md
- 實驗輸出：artifacts/
- 本機資料庫：authentication_history.db（維持不刪）

## 目前缺漏（下一步建議）

1. 尚未做 commit 分流
- 目前工作樹同時有「功能修改」與「清理刪檔」，建議分成兩個 commit：
  - commit A：功能（app.py, mqtt_bridge.py, node.py, docs/checklists/DELIVERY_CHECKLIST.md）
  - commit B：整理（.gitignore + 刪除 challenge_out.json/response_in.json）

2. 尚未補充 2026-04-05 的文件同步
- 建議把「單實例防護」與「手動驗證（免腳本）」補到 docs/guides/MQTT_FIX_GUIDE.md（若要交付展示會更完整）

3. 尚未做最小 smoke 測試紀錄
- 建議至少記錄一次：
  - Bridge 正常
  - Node 正常
  - 一鍵驗證成功
  - SQLite 新增一筆 auth_history

## 今日執行過的指令（重點）

以下為今天在專案內實際執行的主要命令（含整理與盤點）：

1. 環境啟用（多次）
- & c:\Programming\IoT_Security_Project\.venv\Scripts\Activate.ps1

2. 變更檢視
- git diff -- app.py mqtt_bridge.py node.py docs/checklists/DELIVERY_CHECKLIST.md --stat
- git status --short

3. 盤點專案內容
- Get-ChildItem -Name
- Get-ChildItem docs/reports -File | Select-Object -ExpandProperty Name

4. 快取與暫存盤點
- Get-ChildItem -Recurse -Directory -Force | Where-Object { $_.Name -eq '__pycache__' } | Select-Object -ExpandProperty FullName

5. 清理執行期檔案
- $targets = @('__pycache__','bridge_status.json','challenge_out.json','response_in.json'); foreach ($t in $targets) { if (Test-Path $t) { Remove-Item -Recurse -Force $t } }; Get-ChildItem -Name

6. 清理專案快取並再次檢查狀態
- Get-ChildItem -Recurse -Directory -Force | Where-Object { $_.Name -eq '__pycache__' -and $_.FullName -notlike '*\.venv\*' } | Remove-Item -Recurse -Force; git status --short

## 現在可直接做的下一個動作

1. 先提交功能修改
2. 再提交清理修改
3. 啟動三進程做一次手動 smoke 驗證（免腳本）
